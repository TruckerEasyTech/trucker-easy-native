# Local secrets (never commit)
Config/TruckerEasy.secrets.xcconfig

# Python venv (middleware local)
backend/**/.venv/
backend/**/.venv*/
**/__pycache__/
*.pyc

# Xcode / SPM / build caches in repo folder
.derivedData*/
*.xcuserstate

# Cursor / local tooling
.cursor/

# Copied DerivedData inside repo (if present)
.xcodeDerivedData/

# Env files (backend / scripts)
.env
.env.*
!.env.example

# AWS / chaves e certificados (nunca no Git)
.aws/
**/aws_credentials*
**/credentials.bak
*.pem
*.p12
*.p8
*.mobileprovision

# CocoaPods (regenerated by `pod install`)
Pods/

# Node (if any tooling added later)
node_modules/
dist/
build/
