// Base configuration for Release — includes CocoaPods, then optional local secrets.
#include "../Pods/Target Support Files/Pods-trucker easy app/Pods-trucker easy app.release.xcconfig"
#include? "TruckerEasy.secrets.xcconfig"

// dSYM for crash reports (TestFlight / App Store upload)
DEBUG_INFORMATION_FORMAT = dwarf-with-dsym
